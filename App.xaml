<Application x:Class="DeerFlow.WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DeerFlow.WPF"
             xmlns:converters="clr-namespace:DeerFlow.WPF.Converters"
             Startup="Application_Startup">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 布尔值到可见性转换器 -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- 布尔值取反转换器 -->
            <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>

            <!-- 布尔值到状态颜色转换器 -->
            <local:BooleanToBrushConverter x:Key="BooleanToBrushConverter"/>

            <!-- 命令到可见性转换器 -->
            <local:CommandToVisibilityConverter x:Key="CommandToVisibilityConverter"/>

            <!-- 布尔值到状态文本转换器 -->
            <converters:BooleanToStatusTextConverter x:Key="BooleanToStatusTextConverter"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>
