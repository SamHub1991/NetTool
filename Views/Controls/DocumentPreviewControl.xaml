<UserControl x:Class="DeerFlow.WPF.Views.Controls.DocumentPreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Border Background="{StaticResource CardBackground}"
            CornerRadius="12" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题和工具栏 -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="📄 文档预览" FontSize="18" FontWeight="SemiBold"
                           Foreground="{StaticResource TextColor}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ComboBox x:Name="DocumentTypeSelector" Width="150" Margin="0,0,12,0"
                              SelectionChanged="DocumentTypeSelector_SelectionChanged">
                        <ComboBoxItem Content="📊 系统健康报告" Tag="Health"/>
                        <ComboBoxItem Content="📚 模式库文档" Tag="Patterns"/>
                        <ComboBoxItem Content="💡 经验记忆文档" Tag="Experiences"/>
                        <ComboBoxItem Content="📝 反思总结报告" Tag="Reflections"/>
                    </ComboBox>
                    <Button Content="🔄 生成" Style="{StaticResource PrimaryButton}"
                            Padding="16,8" Margin="0,0,8,0"
                            Click="GenerateButton_Click"/>
                    <Button Content="💾 导出" Style="{StaticResource SecondaryButton}"
                            Padding="16,8"
                            Click="ExportButton_Click"/>
                </StackPanel>
            </Grid>

            <!-- 文档内容预览 -->
            <Border Grid.Row="1" Background="{StaticResource BackgroundColor}"
                    CornerRadius="8" BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1" Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Disabled">
                    <TextBox x:Name="DocumentContent"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{StaticResource TextColor}"
                             FontFamily="Consolas, Courier New, monospace"
                             FontSize="13"
                             TextWrapping="Wrap"
                             IsReadOnly="True"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled"/>
                </ScrollViewer>
            </Border>

            <!-- 状态栏 -->
            <Border Grid.Row="2" Margin="0,16,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" x:Name="StatusText"
                               Text="就绪" FontSize="12"
                               Foreground="{StaticResource TextMutedColor}"/>
                    <TextBlock Grid.Column="1" x:Name="WordCountText"
                               Text="0 字" FontSize="12"
                               Foreground="{StaticResource TextMutedColor}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
