# DeerFlow.WPF 开发文档

## 一、项目概述

DeerFlow.WPF 是一款基于 WPF（Windows Presentation Foundation）技术栈构建的桌面 AI 超级智能体应用。项目整合了字节跳动 DeerFlow 2.0 的智能体编排理念和 Lynn 的桌面端记忆系统设计，采用 MVVM 架构模式，集成了 AIO Sandbox 多窗口任务隔离机制。

**版本**: 1.0.0 | **目标框架**: .NET 10.0 (Windows) | **许可证**: MIT

---

## 二、项目结构

```
DeerFlow.WPF/
├── App.xaml / App.xaml.cs           # 应用程序入口 + DI容器初始化
├── MainWindow.xaml / .cs            # 主窗口（自定义WindowChrome标题栏）
│
├── Core/                            # MVVM基础设施
│   ├── ViewModelBase.cs             # INotifyPropertyChanged 基类
│   ├── RelayCommand.cs              # ICommand 实现 + 泛型版本
│   └── AsyncObservableCollection.cs # 支持异步加载的集合
│
├── Models/                          # 数据模型
│   └── Models.cs                    # TaskModel, ChatMessage, MemoryItem, SkillItem, AgentConfig
│
├── Services/                        # 服务层（接口+实现）
│   ├── ApiService.cs                # AI API SSO流式通信 (IApiService)
│   ├── TaskWindowManager.cs         # 多窗口进程管理器 (ITaskWindowManager)
│   ├── SandboxManager.cs            # 文件系统沙箱 (ISandboxManager)
│   ├── WatchdogService.cs           # 进程监控+自动恢复 (IWatchdogService)
│   └── LoggerService.cs             # 文件轮转日志 (ILoggerService)
│
├── ViewModels/                      # 视图模型
│   ├── MainViewModel.cs             # 主窗口ViewModel + MenuItemVM
│   └── PageViewModels.cs            # 7个页面ViewModel
│
├── Views/                           # 视图
│   ├── Controls/                    # 自定义控件
│   │   ├── TitleBarControl.xaml     # WindowChrome 标题栏
│   │   ├── SidebarControl.xaml      # 侧边导航栏
│   │   └── StatusBarControl.xaml    # 状态栏
│   ├── Pages/                       # 7个功能页面
│   │   ├── HomePage.xaml            # 首页（快速入口+特性展示）
│   │   ├── ChatPage.xaml            # AI对话页（流式消息渲染）
│   │   ├── TaskPanelPage.xaml       # 任务面板（AIO沙箱管理）
│   │   ├── AgentOrchestrationPage.xaml # 智能体编排页
│   │   ├── MemoryPage.xaml          # 记忆库页
│   │   ├── SkillsPage.xaml          # 技能商店页
│   │   └── SettingsPage.xaml        # 系统设置页
│   └── Windows/
│       └── TaskWindow.xaml          # 独立任务窗口（沙箱进程入口）
│
├── Themes/
│   └── DarkTheme.xaml               # Trae Solo 暗色主题
│
├── Tests/                           # 单元测试
│   ├── Core/                        # ViewModelBase + RelayCommand 测试
│   └── Services/                    # 全部服务测试
│
├── task_plan.md                     # 任务计划
├── progress.md                      # 进度追踪
├── findings.md                      # 研究发现
└── 可行性研究报告.md                  # 完整可行性研究
```

---

## 三、核心架构设计

### 3.1 MVVM 分层

```
View (XAML) ──绑定──> ViewModel ──调用──> Service ──操作──> Model
    │                    │                    │
    └──DataContext───────┘                    │
                └── DI Container ──────────────┘
```

**关键规则**：
- View 只包含 XAML 声明性绑定和最少代码后端
- ViewModel 不引用任何 UI 控件，只操作数据和命令
- Service 层独立于 UI，可被单元测试完全覆盖
- Model 是纯数据容器（POCO）

### 3.2 依赖注入容器

在 [App.xaml.cs](file:///e:/code/item/DeerFlow.WPF/App.xaml.cs) 中配置，使用 `Microsoft.Extensions.DependencyInjection`：

| 服务 | 生命周期 | 原因 |
|------|---------|------|
| LoggerService | 单例 | 全局唯一日志实例 |
| SandboxManager | 单例 | 全局沙箱根目录管理 |
| TaskWindowManager | 单例 | 全局任务状态统一管理 |
| ApiService | 瞬时 | 每次请求独立 HttpClient |
| 所有 ViewModel | 瞬时 | 每次导航创建新实例 |

### 3.3 页面导航机制

使用 `ContentControl` + `DataTemplate` 实现声明式页面切换：

```xml
<ContentControl Content="{Binding CurrentPage}">
    <ContentControl.Resources>
        <DataTemplate DataType="{x:Type vm:ChatViewModel}">
            <pages:ChatPage/>
        </DataTemplate>
        <!-- 其他7个页面... -->
    </ContentControl.Resources>
</ContentControl>
```

### 3.4 任务窗口隔离架构

```
MainWindow
    │
    ├── TaskPanelPage（任务列表+创建入口）
    │
    └── TaskWindowManager
            │
            ├── 创建 SandboxManager → 沙箱目录
            ├── 创建 Process（DeerFlow.WPF.exe --task-id xxx）
            │       └── TaskWindow（独立窗口）
            │               └── 沙箱文件系统挂载
            │
            ├── WatchdogService（3秒健康检查 + 30秒心跳）
            │       ├── 检测进程退出 → 自动重启（最多3次）
            │       └── 心跳超时 → 触发恢复
            │
            └── 资源监控（CPU/内存每2秒采样）
```

---

## 四、服务层 API 参考

### 4.1 IApiService

```csharp
/// 流式聊天请求
IAsyncEnumerable<string> SendChatStreamAsync(string message, AgentConfig config, string? taskId = null);

/// 获取可用模型列表
Task<List<string>> GetAvailableModelsAsync();

/// 健康检查
Task<bool> HealthCheckAsync();
```

### 4.2 ITaskWindowManager

```csharp
/// 创建独立任务窗口
Task<TaskModel> CreateTaskWindowAsync(TaskModel task);

/// 关闭任务窗口
Task<bool> CloseTaskWindowAsync(string taskId);

/// 获取所有活跃任务
List<TaskModel> GetActiveTasks();

/// 暂停/恢复任务
Task<bool> PauseTaskAsync(string taskId);
Task<bool> ResumeTaskAsync(string taskId);

/// 进程状态变更事件
event Action<TaskModel>? TaskStatusChanged;
```

### 4.3 ISandboxManager

```csharp
/// 创建沙箱（含 workspace/temp/output 子目录）
Task<string> CreateSandboxAsync(string taskId);

/// 销毁沙箱
Task DestroySandboxAsync(string taskId);

/// 获取沙箱文件列表
Task<List<string>> GetSandboxFilesAsync(string taskId);

/// 沙箱内命令执行（白名单控制）
Task<string> ExecuteInSandboxAsync(string taskId, string command);
```

### 4.4 IWatchdogService

```csharp
/// 注册/注销监控
void Register(string taskId, Process process);
void Unregister(string taskId);

/// 健康状态查询
Dictionary<string, ProcessHealth> GetHealthStatus();

/// 启动/停止监控循环
void StartMonitoring();
void StopMonitoring();

/// 进程崩溃事件
event Action<TaskModel>? ProcessCrashed;
```

---

## 五、UI 设计规范

### 5.1 配色方案（Trae Solo 暗色主题）

| 色值 | 用途 |
|------|------|
| `#1E1E24` | 主背景 + 标题栏 + 侧边栏 |
| `#25252C` | 内容区背景 |
| `#2A2A33` | 卡片背景 |
| `#6C5CE7` | 主强调色（按钮、边框焦点） |
| `#E8E8F0` | 主文字色 |
| `#A0A0B0` | 次要文字色 |
| `#707080` | 弱文字色 |

### 5.2 控件样式

- **TextBox**: 圆角 8px + 焦点时紫色边框
- **PrimaryButton**: 紫色填充 + Hover 提亮 + Pressed 透明度
- **SecondaryButton**: 透明背景 + 灰色边框 + Hover 紫色边框
- **ListBox**: 透明背景 + Hover/Selected 高亮

### 5.3 窗口设计

- 使用 `WindowChrome` 实现无边框自定义窗口
- `CaptionHeight="48"` 标题栏区域
- `CornerRadius="10"` 窗口圆角
- `DropShadowEffect` 窗口阴影
- 最大化时自动移除圆角并增加 `Margin="8"` 避免任务栏遮挡

---

## 六、扩展指南

### 6.1 添加新页面

1. 在 `Models/` 中定义数据模型（如需要）
2. 在 `ViewModels/PageViewModels.cs` 中添加 ViewModel
3. 在 `Views/Pages/` 中创建 XAML + code-behind
4. 在 `MainViewModel.InitMenu()` 中添加菜单项
5. 在 `MainViewModel.NavigateTo()` 中添加路由映射
6. 在 `MainWindow.xaml` 的 `DataTemplate` 中添加类型映射

### 6.2 添加新服务

1. 定义接口 → 实现 → 在 `App.xaml.cs` 的 `ConfigureServices()` 中注册
2. 在需要使用的 ViewModel 构造函数中注入接口

### 6.3 集成新的 AI 模型

修改 `SettingsPage` 中的模型配置项，或直接修改 `AgentConfig` 的 `ModelName` 和 `BaseUrl` 属性。

---

## 七、已知限制

1. **仅支持 Windows**: WPF 技术栈限制，不支持 macOS/Linux
2. **沙箱为文件系统级**: 当前版本使用目录隔离，非真正的容器/Docker 隔离
3. **命令白名单有限**: 仅允许 9 种基础命令在沙箱内执行
4. **进程间通信基础**: 暂未实现 IPC/Named Pipe 通信

## 八、依赖项

| 包名 | 版本 | 说明 |
|------|------|------|
| Microsoft.Extensions.DependencyInjection | 8.0.0 | DI容器 |
| Microsoft.Extensions.Http | 8.0.0 | HttpClient工厂 |
| Microsoft.Xaml.Behaviors.Wpf | 1.1.122 | WPF行为扩展 |
| Serilog + Sinks | 3.1.1 | 结构化日志 |
| Microsoft.Data.Sqlite | 8.0.0 | 本地数据存储 |
| xUnit + Moq | 2.7.0 / 4.20.70 | 单元测试 |
