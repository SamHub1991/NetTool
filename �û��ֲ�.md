# DeerFlow.WPF 用户手册

## 欢迎使用 DeerFlow.WPF

DeerFlow.WPF 是一款 Windows 桌面 AI 超级助手，集成了智能对话、多窗口沙箱任务和智能体编排功能。

---

## 一、安装与启动

### 系统要求
- **操作系统**: Windows 10 版本 1809+ 或 Windows 11
- **内存**: 最低 4GB，推荐 8GB+
- **磁盘空间**: 200MB 可用空间
- **依赖**: .NET 10.0 运行时（安装包自动安装）

### 安装步骤
1. 下载 `DeerFlow.WPF.msi` 安装包
2. 双击运行，按向导完成安装
3. 从开始菜单或桌面快捷方式启动

---

## 二、界面导航

### 主窗口布局

```
┌──────────────────────────────────────────────┐
│ 🦌 DeerFlow WPF              [─] [□] [✕]    │ ← 标题栏
├────┬─────────────────────────────────────────┤
│ 🏠 │                                         │
│ 💬 │         主内容区                         │
│ 📋 │    （页面内容随导航切换）                  │
│ 🤖 │                                         │
│ 🧠 │                                         │
│ 🛠 │                                         │
│ ⚙  │                                         │
├────┴─────────────────────────────────────────┤
│ 状态信息             任务: 3        内存: 512MB│ ← 状态栏
└──────────────────────────────────────────────┘
```

### 导航菜单

| 图标 | 功能 | 说明 |
|------|------|------|
| 🏠 | 首页 | 快速操作入口、核心特性展示、最近任务 |
| 💬 | 对话 | AI 对话窗口，支持流式消息渲染 |
| 📋 | 任务面板 | 创建和管理沙箱隔离任务 |
| 🤖 | 智能体编排 | 配置子智能体工作流和中间件链 |
| 🧠 | 记忆库 | 管理 AI 记忆条目和上下文 |
| 🛠 | 技能商店 | 浏览和安装社区技能 |
| ⚙  | 设置 | AI 模型配置、功能开关 |

---

## 三、功能使用指南

### 3.1 AI 对话

1. 点击侧边栏「💬 对话」进入对话页面
2. 在底部输入框输入问题
3. 按 **Enter** 发送（Shift+Enter 换行）
4. AI 回复实时流式显示
5. 点击「清空」按钮重置对话历史

### 3.2 创建沙箱任务

1. 点击侧边栏「📋 任务面板」
2. 输入**任务名称**（必填）和任务描述（可选）
3. 点击「🚀 创建任务」按钮
4. 系统将创建独立沙箱窗口，每个任务拥有：
   - 独立的进程空间
   - 独立的文件系统（workspace / temp / output）
   - 命令白名单安全限制
   - 实时的 CPU 和内存监控

### 3.3 管理任务

在任务面板中可以：
- **暂停/恢复**: 暂停不活跃的任务以释放资源
- **关闭**: 终止任务进程并清理沙箱文件
- **监控**: 实时查看 PID、内存占用、CPU 使用率
- **状态**: 通过颜色标识了解任务状态（绿=运行 / 黄=等待 / 红=失败）

### 3.4 智能体编排

1. 进入「🤖 智能体编排」页面
2. 设置编排名称和步骤描述
3. 配置最大子智能体数（建议 2-5）
4. 查看可用的中间件链（护栏、记忆、循环检测等）
5. 保存编排后可一键启动执行

### 3.5 记忆管理

1. 进入「🧠 记忆库」页面
2. 查看所有已存储的记忆条目
3. 每条记忆包含：
   - 类型标签（事实 / 偏好 / 上下文）
   - 重要性评分（1-10星）
   - 创建时间戳
4. 使用搜索框快速查找相关记忆

### 3.6 技能商店

- 浏览预置技能（深度研究、代码审查、前端设计等）
- 未安装的技能显示「⬇ 安装」按钮
- 已安装的技能显示「✓ 已安装」

---

## 四、设置配置

### AI 模型配置
| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 模型提供方 | openai | OpenAI 兼容的 API |
| API Base URL | http://localhost:11434/v1 | API 地址 |
| API Key | (空) | 留空使用本地推理模式 |

### 功能开关
- **本地推理优先**: 启用后优先使用本地模型，保护隐私
- **Sandbox 隔离**: 启用 AFS Sandbox 模式，每个任务独立进程
- **自动保存记忆**: AI 对话中的关键信息自动存入记忆库

---

## 五、常见问题

**Q: 对话无响应？**
A: 检查「设置」中 API Base URL 是否正确。如使用本地推理，确保 Ollama / vLLM 服务已启动。

**Q: 创建任务后窗口闪退？**
A: 任务窗口需要完整 WPF 运行时环境。确保已安装 .NET 10.0 运行时，查看日志了解具体错误。

**Q: 内存占用过高？**
A: 在任务面板中关闭不活跃的任务，或可在设置中降低本地推理的并发数。

**Q: 如何报告问题？**
A: 请提交 Issue 至项目 GitHub 仓库，附上日志文件（位于 `%LocalAppData%\DeerFlow.WPF\logs\`）。

---

## 六、快捷键

| 快捷键 | 功能 |
|--------|------|
| Enter | 发送消息（输入框内） |
| Shift+Enter | 换行（输入框内） |
| Alt+← | 返回上一页 |
| F5 | 刷新当前页面 |

---

## 七、技术支持

- **文档**: 参见 `开发文档.md`
- **日志**: `%LocalAppData%\DeerFlow.WPF\logs\`
- **沙箱数据**: `%LocalAppData%\DeerFlow.WPF\sandboxes\`
